# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_LOG_LEVEL=info

# Model & Data Paths (can be volume-mounted in Docker)
MODEL_PATH=models/final_random_forest_model.joblib
SCALER_PATH=models/scaler.joblib
THRESHOLD_PATH=models/threshold.json

# Mitigation Engine Settings
MITIGATION_PROFILE=prod
MITIGATION_PROBABILITY_THRESHOLD=0.57
MITIGATION_RATE_LIMIT=200
MITIGATION_BLOCK_TTL_SECONDS=300

# Logging Settings
LOG_DIR=logs
LOG_LEVEL=INFO

# Optional: External Integrations (if added)
# SLACK_WEBHOOK_URL=https://hooks.slack.com/...
# PAGERDUTY_KEY=...
# PROMETHEUS_PUSHGATEWAY=...

# Optional: Database (if persistent storage added)
# DB_URL=postgresql://user:pass@host/db
